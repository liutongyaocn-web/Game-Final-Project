# Assessment Mapping

| Coursework evidence area | Planned Last Stand evidence | Current status |
|---|---|---|
| Game story and logic | Lone survivor in an apocalypse arena, 5-wave survival loop, extraction/win condition, death/lose condition. | Story and loop documented; extraction/victory foundation exists, and real enemy-damage player death has been manually validated into the Failed state. |
| UI and statistics | JU TPS default UI is now present and active in `LS_Arena_01` as the base interface; custom Last Stand HUD extensions display wave, enemies remaining, score, kills, survival timer, FPS, objective text, player health when readable, and end-state panels; `LS_MainMenu` provides a project-owned start menu and controls panel. | Last Stand HUD layer is implemented and validated through live UI values; enemy count now reads `Enemies Remaining: X / Total` for clearer wave progress; Task 24 added a dark stats panel and objective banner, Task 24R reduced them, Task 24S made the overlay minimal, Task 24U reworked it into compact combined rows for Full HD gameplay recording, and Task 24V removed the visible background rectangles; manual failure validation showed health `0 / 400` and objective `You died`; the end screen now displays visible victory/failure titles, run stats, and restart prompt using the active input system; the main menu scene exists with Start/Controls/Quit UI and a complete scene-loading setup. |
| Game mechanics | JU TPS provides base controller, camera, aiming, shooting, reload, inventory, health/damage, and interaction foundations; Last Stand code provides wave rules, enemy death drops, objectives, win/lose flow, restart integration, and enemy balance/pacing tuning. | Player control and camera foundation manually validated; enemy defeat contributes to kills and score; extraction victory exists; real player-death failure has been manually validated; defeated enemies now drop health/ammo supplies; victory/failure states are communicated through an end screen with restart prompt; ranged enemy distance pressure and final wave pacing were tuned for fairness and demo usability. |
| Game AI | JU TPS provides base AI movement/detection/chase/attack/damage behaviours; Last Stand code provides enemy type wrappers, spawning, wave difficulty, score, kills, and death reporting. | WaveManager can request runtime enemies through SpawnDirector; spawned JU TPS enemies bind to `Player_JUTPS`, act through their validated AI behaviours, and now include EnemyLifecycleReporter bridge support. |
| Animation | JU TPS provides player locomotion/combat/reload/ragdoll and compatible zombie movement/attack/hit/death animation foundations. | Fist melee, blade melee, and ranged/gun attack behaviours have been validated enough for later video demonstration. |
| Content generation | Last Stand wave generation will use weighted enemy selection, spawn point selection, max-alive limits, spawn timing, difficulty scaling, and dynamic resource drops from defeated enemies; Codex-assisted planning/scripts are logged and reviewed. | WaveManager generates enemies over time from WaveDefinition data through SpawnDirector, with max-alive control validated for Wave 1. EnemyDeathDropper generates health/ammo pickup prefabs from enemy defeat events, and manual validation confirmed pickup spawning and collection. |
| Level progression | POLYGON Apocalypse `Demo_City_Universal_RenderPipeline.unity` is the recommended source for a cropped quarantine checkpoint / military evacuation roadblock arena; Last Stand code provides escalating waves 1-5, combat pacing, extraction endpoint, and objective progression. | WaveManager can execute configured waves; final-wave completion now signals GameFlowManager to unlock extraction; final WaveDefinition pacing was shortened while retaining all five waves. Full Wave 5 manual validation remains. |
| External assets and attribution | POLYGON Apocalypse is used for environment/map/props only; JU TPS is used for gameplay foundation; attribution is tracked separately from student-authored systems. | Attribution updated; raw third-party source assets remain uncommitted in this task. |
| Runtime stability | Last Stand should avoid red Console errors during validation and final demo, while treating normal third-party informational logs separately. | JU TPS enemy audio-tag assertion was fixed and manually revalidated; enemy drop/pickup validation completed cleanly; the end-screen restart input exception was fixed for the New Input System; Task 18.5 MCP smoke audit reported 0 red errors/warnings across static, wave, ranged, failed, and victory checks. Task 20 found no Last Stand gameplay exception after final pacing, but Unity MCP's own live serializer produced red tooling entries while inspecting JU TPS/Animator/audio internals. Task 22D fixed the JU TPS pause menu scene target after the Last Stand main menu replaced the old demo menu build flow. Task 22D-R resets cursor/time/UI state on menu load so returning from gameplay remains clickable. Task 24R investigated a `UnityEditor.EditorStyles.get_toolbarButtonRight` red stack trace and found no `_LastStand`, `LastStand`, or old Input System exception, so it is treated as Editor Inspector/UIElements noise rather than gameplay failure. |
| Testing and iteration | Playtest observations should feed back into tuning, documentation, and final demo preparation. | Manual playtest feedback led to ranged attack distance reduction, restart validation notes, and shorter final wave pacing. Task 18.5 adds automated evidence for scene wiring, Wave 1 spawn behaviour, ranged enemy runtime state, failed/victory end states, and remaining manual checks. Task 20 confirms the final auto-start pacing state in normal Play Mode: Wave 1 begins automatically, HUD/stats update, enemies spawn, and runtime enemy wiring remains intact. |
| GitHub release/version control | Small verified commits, clean scope, documentation logs, final release before submission. | Main branch contains the project-owned Last Stand implementation and evidence logs. Task 21 adds a release-readiness checklist; final packaging still needs explicit third-party asset/build access notes because raw imported asset folders are not intended for public source commits. |
| C# code quality | Modular custom systems under `Assets/_LastStand`, events for decoupling, ScriptableObjects for data, pooling for repeated spawning/effects where useful, and integration wrappers rather than duplicated JU TPS systems. | Modular WaveManager/SpawnDirector/StatsManager/HUD/GameFlow separation is implemented. Enemy drops are separated into `EnemyDropTable`, `EnemyDeathDropper`, and the `EnemyLifecycleReporter.Defeated` event. `EndScreenController` is separated from `GameFlowManager` and only presents end-state UI/restart behaviour. |

## Final Readiness Notes
- Completed systems now cover the required vertical slice: player foundation, arena, AI enemies, wave spawning, stats, HUD, pickups, win/fail state, and restart foundation.
- The project now also has a project-owned main menu scene with controls information and scene-loading setup.
- Task 22C found no ready-made JU TPS/JUTPS Addons minimap or radar system; the current HUD remains the recommended UI evidence, with minimap work postponed to avoid late custom-system risk.
- Task 23A found that revive-current-wave is feasible but touches multiple core systems; it should be treated as optional gameplay polish unless there is enough time for full manual validation.
- Task 24 improved HUD readability without changing gameplay logic; Tasks 24R, 24S, 24U, and 24V iterated the layout into a compact Full HD gameplay HUD with no visible background panels after visual feedback; Task 24W removed duplicate FPS from the custom HUD and centred the end-screen layout away from the JU TPS weapon UI; Task 24X enlarged the end-screen card/hierarchy for clearer win/fail communication.
- Remaining risks are acceptable for submission if documented and demonstrated carefully: full Wave 5 extraction validation, victory-state restart, ranged feel, possible NavMesh stuck spots, plain HUD visuals, and third-party asset delivery.
- Recommended evidence priority is now video/demo clarity rather than major new implementation.


