# Post-Pacing Smoke Audit

## Purpose
Task 20 validates the final gameplay pacing changes from Task 19. The goal was to confirm that `LS_Arena_01` now starts the Last Stand wave loop automatically, that Wave 1 begins spawning enemies without manual setup, and that the final scene remains safe for demo use.

## Static Scene and Config Audit
- Scene checked: `Assets/_LastStand/Scenes/LS_Arena_01.unity`.
- `WaveManager.autoStartOnPlay` is `true` for the final playable scene.
- `SpawnDirector.debugSpawnOnStart` is `false`.
- `PlayerDeathMonitor.debugReportDeathOnStart` is `false`.
- `GameFlowManager` debug extraction toggles are `false`.
- `EndScreen_Panel` is inactive by default.
- WaveManager still references five WaveDefinition assets.
- SpawnDirector references `_SpawnPoints`, `_Systems/Spawned_Enemies`, and `Player_JUTPS`.
- GameFlowManager references WaveManager, StatsManager, and ExtractionObjective.
- HUD references StatsManager, WaveManager, GameFlowManager, FPS counter, and PlayerHealthReader.

## Auto-Start Play Mode Result
Play Mode was entered through Unity MCP after the pacing changes. Wave 1 started automatically without manually selecting the WaveSystem or enabling a temporary debug toggle.

Observed WaveManager state during the smoke run:
- State: `Spawning`
- Current wave: `1 / 5`
- Enemies spawned this wave: `2 / 3`
- Total enemies this wave: `3`
- Alive enemy count: `2`
- Wave 1 `maxAliveAtOnce` was respected at `2`.

## HUD and Stats Result
The HUD/stat systems updated during the automatic Wave 1 run:
- Current wave displayed through runtime state as `1 / 5`.
- Enemy counts updated to `2 / 3` during the sampled runtime state.
- Survival time increased to approximately `00:39`.
- FPS updated at runtime, observed around `73`.
- Player health was readable as `400 / 400`.

## Enemy Spawn and Targeting Result
Two Wave 1 fist melee enemies spawned automatically under `_Systems/Spawned_Enemies`.

Inspected spawned enemy components confirmed:
- `EnemyTargetBinder` was present.
- JU TPS AI target was assigned to `Player_JUTPS`.
- `EnemyLifecycleReporter` was present and configured.
- `EnemyDeathDropper` was present with the default drop table.
- `SpawnedEnemyRuntimeInfo` was present and configured with `EnemyDefinition_FistMelee`, wave `1`, and score value `100`.
- The inspected enemy was moving/running through JU TPS AI behaviour.

## Console Result
No Last Stand gameplay exception, JU TPS audio tag assertion, or script compile error was observed during the post-pacing smoke run.

Important note: the Unity Console did contain red entries generated by Unity MCP's own live GameObject/component serialization while inspecting runtime JU TPS/Animator/audio internals. Those entries referenced files under `Library/PackageCache/com.coplaydev.unity-mcp...`, including Animator IK/property access and TransformHandle serialization messages. They are recorded as tooling noise from the automated inspection path, not as Last Stand gameplay errors.

Regular JU TPS `SwitchID` inventory logs appeared and remain acceptable.

## Victory and Restart Result
The victory debug path was not re-tested in Task 20. It had already passed in Task 18.5, and this task avoided scene/debug-setting changes beyond normal Play Mode smoke testing.

Restart validation status:
- Failed-state `R` restart was previously manually validated after the New Input System fix.
- Victory-state `R` restart remains a manual follow-up because full victory flow is slower to reach and MCP keyboard simulation was not used.

## Cleanup Result
- Play Mode was exited.
- No runtime-spawned enemies or pickups were saved into the scene.
- Final `WaveManager.autoStartOnPlay` remains `true`, as intended after Task 19.
- Git status after the smoke run showed no modified scene/script/prefab assets from this task; only pre-existing untracked imported assets/ProjectSettings remained.

## Remaining Manual Checks
- Full Wave 5 clear through normal play.
- Extraction trigger through real play after the final wave.
- Victory-state `R` restart.
- Final ranged enemy feel after the distance tuning.
- NavMesh stuck spots in detailed map areas.
- Final HUD/end-screen visual polish if time allows.

## Recommendation
Proceed with targeted final manual playtesting for the remaining full-run checks rather than adding new systems. The core post-pacing start flow is now verified: pressing Play starts Wave 1 automatically, spawns enemies, updates HUD/stat state, and keeps runtime enemy wiring intact.
